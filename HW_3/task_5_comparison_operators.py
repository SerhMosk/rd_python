# Task 5. Create a variable with the value 1. Using comparison operators,
# compare it with any other values (minimum 5 comparisons) and check the
# output in the interpreter.
int_var = 1

print(f"int_var == 'int_var': {int_var == 'int_var'}")

print(f"int_var != 1: {int_var != 1}")

print(f"int_var > 0: {int_var > 0}")

print(f"int_var < 2: {int_var < 2}")

print(f"int_var >= 0: {int_var >= 0}")

print(f"int_var <= 1.0: {int_var <= 1.0}")
